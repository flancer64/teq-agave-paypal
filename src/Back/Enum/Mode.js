/**
 * Enum representing the mode (sandbox or production) for PayPal integration.
 *
 * @see Environment (@paypal/paypal-server-sdk/src/configuration.ts)
 */
const Fl64_Paypal_Back_Enum_Mode = {
    SANDBOX: 'SANDBOX',
    PRODUCTION: 'PRODUCTION',
};
Object.freeze(Fl64_Paypal_Back_Enum_Mode);
export default Fl64_Paypal_Back_Enum_Mode;